"""Backward-compatible config re-export module for Neuro-CXG.

All public names from submodules are re-exported here for convenience.
Use ``from src.core.config import <name>`` to access any config constant.
"""

import logging

# ruff: noqa: F401 — all imports here are re-exports consumed by other modules
from .atlas_config import (
    AAL3_VALID_ROI_RANGE,
    LOBE_MAPPING,
    LOBE_NAMES,
    LOBE_TO_NETWORK,
    NETWORK_NAMES,
    NETWORK_TO_LOBES,
    NUM_LOBES,
    NUM_NETWORKS,
    ROI_CENTROIDS_PATH,
    SPATIAL_MIN_REQUIRED_REGIONS,
    USE_11_LOBES,
)
from .feature_registry import (
    ACTIVE_FREQ_BANDS,
    ALFF_SLICE_PERCENTILES,
    ALL_FEATURE_NAMES,
    BANDPASS_HIGH,
    BANDPASS_LOW,
    DEFAULT_TR,
    EXCLUDE_NYQUIST_BANDS,
    FEATURE_GROUPS,
    FREQ_BANDS,
    GNN_IN_CHANNELS,
    NUM_FREQUENCY_FEATURES,
    NUM_SPATIAL_FEATURES,
    NUM_TEMPORAL_FEATURES,
    NYQUIST_EPS,
    SITE_TR_MAP,
    UNRELIABLE_FREQ_BANDS_AT_NYQUIST,
)
from .hyperparams import (
    AUDIT_MAX_EMPTY_ROI_FRACTION,
    AUDIT_SAMPLE_PNG,
    AUDIT_SAMPLE_TS,
    CAUSALITY_METHOD,
    CONFIDENCE_LOG_EPS,
    DEMO_AGE_CENTER,
    DEMO_AGE_SCALE,
    DEMO_FIQ_CENTER,
    DEMO_FIQ_SCALE,
    DEMO_SEX_CENTER,
    DEVICE,
    EVAL_FIXED_THRESHOLD,
    EVAL_THRESHOLD_POLICY,
    EXCLUDED_SUBJECTS,
    FISHER_Z_EPS,
    FOCAL_LOSS_ALPHA,
    FOCAL_LOSS_GAMMA,
    GNN_AUTO_GRL_GRID_SEARCH,
    GNN_BATCH_SIZE,
    GNN_DROPOUT,
    GNN_EARLY_STOPPING_PATIENCE,
    GNN_EDGE_CONTRASTIVE_WEIGHT,
    GNN_EDGE_GATE,
    GNN_ENFORCE_GRAPH_QUALITY_GATE,
    GNN_ENFORCE_MULTIVIEW_QUALITY_GATE,
    GNN_EPOCHS,
    GNN_FOLD_PREPROCESSING_MODE,
    GNN_GRL_ALPHA,
    GNN_GRL_ALPHA_MAX,
    GNN_HIDDEN_CHANNELS,
    GNN_INVARIANCE_WEIGHT,
    GNN_MAX_DEGENERATE_GRAPH_RATE,
    GNN_MI_FEATURE_SELECTION_ENABLED,
    GNN_MI_MAX_KEEP_RATIO,
    GNN_MI_MIN_KEEP_RATIO,
    GNN_MIN_EDGES_FOR_NONDEGENERATE,
    GNN_MIN_EPOCHS_BEFORE_STOPPING,
    GNN_MULTIVIEW_MAX_ZERO_EDGE_RATE,
    GNN_MULTIVIEW_QUALITY_SAMPLE_SIZE,
    GNN_NODE_EMB_DIM,
    GNN_NUM_HEADS,
    GNN_NUM_LAYERS,
    GNN_ONECYCLE_MAX_LR,
    GNN_ONECYCLE_PCT_START,
    GNN_ONECYCLE_WARMUP_FRACTION,
    GNN_POOLING,
    GNN_SEED,
    GNN_SITE_EMBEDDING_DIM,
    GNN_SITE_LOSS_WEIGHT,
    GNN_SITE_NORMALIZATION_MODE,
    GNN_SPATIAL_INVARIANCE_WEIGHT,
    GNN_STRUCTURAL_DROPOUT_PROB,
    GNN_USE_DEMOGRAPHICS,
    GNN_USE_GRL,
    GNN_USE_SITE_EMBEDDING,
    GNN_WEIGHT_DECAY,
    GRANGER_MAX_LAG_SECONDS,
    GRANGER_SIGNIFICANCE_LEVEL,
    GRAPH_DENSITY_TARGET,
    GRL_ANNEAL_STEEPNESS,
    HARMONIZATION_UNSEEN_SITE_POLICY,
    K_FOLDS,
    LAGGED_PEARSON_CONFIDENCE_ALPHA,
    LAGGED_PEARSON_LAGS,
    LAGGED_PEARSON_P_PRUNE_THRESHOLD,
    LAGGED_PEARSON_P_SELECT_THRESHOLD,
    MAX_NAN_ROIS,
    MIN_EDGES_PER_GRAPH,
    MULTIVIEW_GENERATION_ENFORCE_QUALITY_GATE,
    MULTIVIEW_GENERATION_MAX_ZERO_EDGE_RATE,
    MULTIVIEW_GENERATION_POLICY,
    PARTIAL_CORR_FDR_ALPHA,
    PARTIAL_CORR_FDR_ENABLED,
    PARTIAL_CORR_GLASSO_ALPHA,
    PARTIAL_CORR_GLASSO_MAX_ITER,
    PARTIAL_CORR_GLASSO_TOL,
    PARTIAL_CORR_MIN_ABS_EDGE,
    PARTIAL_CORR_MIN_SAMPLES,
    RIDGE_GRANGER_CONFIDENCE_ALPHA,
    RIDGE_GRANGER_HIGH_CONF_P_THRESHOLD,
    RIDGE_GRANGER_HYBRID_BETA,
    RIDGE_GRANGER_LAGS,
    RIDGE_GRANGER_LAMBDA,
    RIDGE_GRANGER_P_PRUNE_THRESHOLD,
    SITE_ROBUSTNESS_GATE_ENABLED,
    SITE_ROBUSTNESS_GATE_POLICY,
    SITE_ROBUSTNESS_MAX_WEAK_SITE_FRACTION,
    SITE_ROBUSTNESS_MIN_EVALUABLE_SITES,
    SITE_ROBUSTNESS_MIN_SITE_AUC,
    SPARSITY_METHOD,
    SPARSITY_QUANTILE,
    SPARSITY_TOPK_PER_NODE,
    USE_CLASS_WEIGHTS,
    USE_FOCAL_LOSS,
    YOLO_BATCH_SIZE,
    YOLO_CONF_THRESHOLD,
    YOLO_DEGREES,
    YOLO_EPOCHS,
    YOLO_FLIPLR,
    YOLO_FLIPUD,
    YOLO_HSV_H,
    YOLO_HSV_S,
    YOLO_HSV_V,
    YOLO_IMGSZ,
    YOLO_MODEL_SIZE,
    YOLO_MOSAIC,
    YOLO_PROJECT_NAME,
    YOLO_TRAIN_CONFIG,
    YOLO_WEIGHTS_PATH,
)
from .paths import (
    ATLAS_METADATA,
    ATLAS_PATH,
    BASELINE_CHECKPOINT_DIR,
    CAUSAL_GRAPHS_DIR,
    CAUSAL_GRAPHS_MULTIVIEW_DIR,
    CHECKPOINT_DIR,
    CONFIG_BRAIN_YAML,
    CONFIG_DIR,
    DATA_ATLASES,
    DATA_FINAL,
    DATA_IMAGES,
    DATA_METADATA,
    DATA_PROCESSED,
    DATA_ROOT,
    DATA_TIME_SERIES,
    FINAL_TEST,
    FINAL_TRAIN,
    FINAL_VAL,
    HARMONIZED_FOLDS_DIR,
    MASTER_MANIFEST,
    MODEL_ROOT,
    NODE_ATTRIBUTES_HARMONIZED,
    NODE_ATTRIBUTES_TEMPORAL,
    NODE_FEATURES_3D,
    NODE_FEATURES_3D_HARMONIZED,
    PHENO_PATH,
    PROJECT_ROOT,
    RESULTS_ABLATIONS_DIR,
    RESULTS_DATA_QUALITY_DIR,
    RESULTS_DIR,
    RESULTS_EVALUATION_DIR,
    RESULTS_TRAINING_DIR,
)
from .validators import (
    get_active_checkpoint_dir,
    summarize_graph_degeneracy_from_adj,
    summarize_graph_degeneracy_from_edge_index,
    validate_environment,
    validate_gnn_training_inputs,
    validate_lobe_mapping,
)

# Derived convenience constants
REGION_LABELS: list[str] = [LOBE_NAMES[i] for i in range(NUM_LOBES)]

logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)


if __name__ == "__main__":
    validate_environment()
