# Development Environment Setup

## Python
- Python 3.12
- Virtual environment using venv

## GPU
- NVIDIA GPU with CUDA 12.x
- Verified using PyTorch CUDA checks

## Setup Steps
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```
