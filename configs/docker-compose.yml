services:
  neuro-cxg:
    build: .
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
    volumes:
      - ./data:/workspace/data
      - ./results:/workspace/results
    working_dir: /workspace
    command: python src/run_pipeline.py --auto
